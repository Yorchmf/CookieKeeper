# Dev environment (Hetzner VPS, compose project complyr-dev, dev.* subdomains).
complyr:
  mail:
    # Deliver via Brevo's EU HTTP API (BREVO_API_KEY must be set; the bean fails fast if not).
    provider: brevo

logging:
  level:
    com.complyr: DEBUG
