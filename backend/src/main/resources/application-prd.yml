# Production environment (Hetzner VPS, compose project complyr-prd).
cookiekeeper:
  mail:
    # Deliver via Brevo's EU HTTP API (BREVO_API_KEY must be set; the bean fails fast if not).
    provider: brevo

logging:
  level:
    root: WARN
    com.complyr: INFO
