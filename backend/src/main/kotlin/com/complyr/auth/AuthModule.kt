package com.complyr.auth

/**
 * Auth feature: registration, login, JWT (access 15 min + refresh rotation), email verification.
 *
 * Placeholder marker — implementation lands in Week 2 (docs/ARCHITECTURE.md, section 13).
 */
object AuthModule
