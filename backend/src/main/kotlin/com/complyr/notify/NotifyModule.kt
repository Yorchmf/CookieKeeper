package com.complyr.notify

/**
 * Notify feature: transactional email via Brevo (verification, receipts, scan-complete).
 *
 * Placeholder marker — implementation lands in Week 6 (docs/ARCHITECTURE.md, section 13).
 */
object NotifyModule
