package com.complyr.consent

/**
 * Consent feature: consent event ingestion (public), audit log queries, CSV export, retention job.
 *
 * `consent_events` is append-only audit evidence — never UPDATE or DELETE from application code.
 * Placeholder marker — implementation lands in Week 3 (docs/ARCHITECTURE.md, section 13).
 */
object ConsentModule
