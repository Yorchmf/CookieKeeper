environment    = "prd"
app_host       = "app.cookiekeeper.eu"
api_host       = "api.cookiekeeper.eu"
cdn_host       = "cdn.cookiekeeper.eu"
marketing_host = "cookiekeeper.eu"

widget_config_ttl_seconds = 300
policy_page_ttl_seconds   = 3600

# cloudflare_zone_id and origin_ipv4 are supplied by the pipeline via TF_VAR_*.
